anthropic>=0.40.0
openpyxl>=3.1.0

# Fine-tuning dependencies (H100 GPU, CUDA 12.1+)
torch>=2.4.0
transformers>=4.45.0
peft>=0.13.0
trl>=0.15.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
datasets>=3.0.0
huggingface_hub>=0.24.0
wandb
openai>=1.40.0
pandas>=2.0.0
tqdm>=4.66.0

# vLLM — fast generation via PagedAttention (replaces HF generate in GRPO)
vllm>=0.6.0
