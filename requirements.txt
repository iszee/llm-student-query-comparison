anthropic>=0.40.0
openpyxl>=3.1.0

# Fine-tuning dependencies (H100 GPU, CUDA 12.1+)
torch>=2.4.0
transformers>=4.45.0
peft>=0.13.0
trl>=0.15.0
accelerate>=0.34.0
datasets>=3.0.0
huggingface_hub>=0.24.0
wandb
openai>=1.40.0
pandas>=2.0.0
tqdm>=4.66.0

# vLLM — fast generation via PagedAttention (replaces HF generate in GRPO)
vllm>=0.6.0

# RAG dependencies (retrieval-augmented generation evaluation variant)
pypdf>=4.0.0                  # PDF text extraction
requests>=2.31.0              # URL fetching
beautifulsoup4>=4.12.0        # HTML cleaning
faiss-cpu>=1.8.0              # dense vector index (FAISS IndexFlatIP)
rank_bm25>=0.2.2              # sparse BM25 retrieval
sentence-transformers>=3.0.0  # BGE-M3 embedder + bge-reranker-v2-m3 cross-encoder
