anthropic>=0.40.0
openpyxl>=3.1.0

# Fine-tuning dependencies (H100 GPU)
torch>=2.4.0
transformers>=4.45.0
peft>=0.13.0
trl>=0.12.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
datasets>=3.0.0
wandb
openai>=1.40.0
flash-attn>=2.6.0
vllm>=0.6.0

# Unsloth — install with the command matching your CUDA version (see README)
# pip install "unsloth[cu121-torch240] @ git+https://github.com/unslothai/unsloth.git"
unsloth
